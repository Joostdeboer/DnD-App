import { Text } from '@/src/components/atoms/generic/Text';

export default async function Songs() {
    return (
        <Text>
            If I ever make or add music specifically made to match the theme of a location, area, landmark, or anything
            of that nature, I will add it here, but for now this remains empty.
        </Text>
    );
}
